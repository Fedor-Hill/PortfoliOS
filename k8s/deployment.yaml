apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-os-angular-app-deployment
spec:
  replicas: 2 
  selector:
    matchLabels:
      app: portfolio-os
  template:
    metadata:
      labels:
        app: portfolio-os
    spec:
      containers:
      - name: angular-container
        image: portfolioos.azurecr.io/portfolio-os@sha256:8373e681c791f987dcb471904b303e63ca0a765f1e8f9cc22efec4234ec57560
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: acr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: portfolio-os-angular-app-service
spec:
  type: LoadBalancer
  selector:
    app: portfolio-os
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
